name: Python CI

on:
  pull_request:
    branches: [ "*" ]
  push:
    branches: [ "*" ]

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.10'
        cache: 'pip'
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install ruff
    - name: Run Ruff linter and formatter
      run: |
        ruff check . --config pyproject.toml

  security:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.10'
        cache: 'pip'
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install bandit[toml]
    - name: Run Bandit security check with detailed report
      run: |
        bandit -c pyproject.toml -r ./ -f html -o bandit-report.html
    - name: Upload security reports
      uses: actions/upload-artifact@v4
      with:
        name: security-reports
        path: |
            bandit-report.html

#   test:
#     runs-on: ubuntu-latest
#     needs: lint
#     steps:
#     - uses: actions/checkout@v4
#     - name: Set up Python
#       uses: actions/setup-python@v5
#       with:
#         python-version: '3.10'
#         cache: 'pip'
#     - name: Install dependencies
#       run: |
#         python -m pip install --upgrade pip
#         pip install pytest pytest-cov
#         pip install -e .
#     - name: Test with pytest
#       run: |
#         pytest --cov=./ --cov-report=xml
#     - name: Verify 100% test coverage
#       run: |
#         python scripts/verify_test_coverage.py
#     - name: Upload coverage report
#       uses: codecov/codecov-action@v3

  pr-status-check:
    runs-on: ubuntu-latest
    if: github.event_name == 'pull_request'
    needs: [lint, security]
    steps:
      - name: PR is ready to be merged
        run: |
          echo "All checks passed! PR is ready to be reviewed and merged."
